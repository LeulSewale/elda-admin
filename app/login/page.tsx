export const metadata = {
  title: "Login | ELDA System Admin",
  description: "Login to access the ELDA System Admin dashboard and manage requests, employees, documents, and users.",
  keywords: ["login", "ELDA System", "admin", "authentication"],
}
import  LoginPageClient  from "./LoginPageClient"

export default function LoginPage() {
  return <LoginPageClient />
}

